<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>Windows Weather</string>

    <key>CFBundleDisplayName</key>
    <string>Windows Weather</string>

    <key>CFBundleIdentifier</key>
    <string>com.github.barnminecraft01.windowsweather</string>

    <key>CFBundleExecutable</key>
    <string>WindowsWeather</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <key>CFBundleShortVersionString</key>
    <string>1.0.0</string>

    <key>CFBundleVersion</key>
    <string>1.0.0</string>

    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <key>LSApplicationCategoryType</key>
    <string>public.app-category.weather</string>

    <!-- Permissive floor so people on older macOS can at least try it. Microsoft's
         supported matrix for .NET 10 starts at macOS 14; anything below that runs
         at your own risk and is untested here. -->
    <key>LSMinimumSystemVersion</key>
    <string>13.0</string>

    <!-- Without this the window is bitmap-scaled on Retina displays and the
         custom-drawn panels look soft. -->
    <key>NSHighResolutionCapable</key>
    <true/>

    <key>NSSupportsAutomaticGraphicsSwitching</key>
    <true/>

    <!-- The app is a normal windowed application, not an agent. -->
    <key>LSUIElement</key>
    <false/>
</dict>
</plist>
